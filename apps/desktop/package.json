{
  "name": "@thrustline/desktop",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "check": "cargo fmt --manifest-path src-tauri/Cargo.toml --check && cargo check --manifest-path src-tauri/Cargo.toml --locked && cargo clippy --manifest-path src-tauri/Cargo.toml --locked -- -D warnings",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml --locked && powershell -NoProfile -ExecutionPolicy Bypass -File ../../tests/desktop-shell/run.ps1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4"
  }
}
